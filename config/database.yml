default: &default
  adapter: postgresql
  encoding: unicode
  username: gschool
  pool: 5

development:
  <<: *default
  database: warmup_activerecord_development

test:
  <<: *default
  database: warmup_activerecord_test